# Secrets — never commit (per project constraint: no API keys in repo)
.env
.env.*
!.env.example
*.key

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.pytest_cache/

# OS
.DS_Store

# Local-only research notes and vendored tooling (not for shared repo)
research/
tools/
.agents/
skills-lock.json

# GSD planning is per-person/local — keep it out of the shared repo
# (lets Tony re-run GSD without injecting planning docs or CLAUDE.md changes)
.planning/

# Audio caches (pre-cached demo lines may be large)
*.wav.tmp

# Generated locally by open-dashboard.command (per-person, not shared)
docs/activity.json
